<tr id="<%= dom_id(rft_volunteer_unsubscribe) %>" >

  <% rft_volunteer_unsubscribe.main_attributes.each do |attr_name| %>
  <td><%= show_attr(rft_volunteer_unsubscribe.public_send attr_name) %></td>
  <% end %>

  <td class="index_table-actions">
    <%= link_to_show(rft_volunteer_unsubscribe) %>
    <%= link_to_edit(rft_volunteer_unsubscribe) %>
<% if rft_volunteer_unsubscribe.try(:archived?) %>
    <%= link_to_recover(rft_volunteer_unsubscribe) %>
<% else %>
    <%= link_to_destroy(rft_volunteer_unsubscribe) %>
<% end %>
  </td>
</tr>
