<div class="address-block well">
  <%= simple_form_for(@address, default_form_options) do |f| %>

      <div class="form-inputs">
        <%= render partial: 'form_fields', locals: {f: f } %>
      </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
  <% end %>
</div>