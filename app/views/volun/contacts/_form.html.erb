<div class="well">
  <%= simple_form_for(@volun_contact, default_form_options) do |f| %>

  <div class="form-inputs">
          <%= f.association :volunteer %>
          <%= f.association :contact_result %>
          <%= f.association :project %>
          <%= f.association :technician %>
          <%= f.input :contact_date, as: :datepicker %>
          <%= f.input :comments %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
  <% end %>
</div>