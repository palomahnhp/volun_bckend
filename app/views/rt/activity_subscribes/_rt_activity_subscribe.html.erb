<tr id="<%= dom_id(rt_activity_subscribe) %>" >

  <% rt_activity_subscribe.main_attributes.each do |attr_name| %>
  <td><%= show_attr(rt_activity_subscribe, attr_name) %></td>
  <% end %>

  <td class="index_table-actions">
    <%= link_to_show(rt_activity_subscribe) %>
    <%= link_to_edit(rt_activity_subscribe) %>
<% if rt_activity_subscribe.try(:archived?) %>
    <%= link_to_recover(rt_activity_subscribe) %>
<% else %>
    <%= link_to_destroy(rt_activity_subscribe) %>
<% end %>
  </td>
</tr>
