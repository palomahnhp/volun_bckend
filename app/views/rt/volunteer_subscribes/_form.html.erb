<div class="well">
  <%= simple_form_for(@rt_volunteer_subscribe, default_form_options) do |f| %>

  <div class="form-inputs">
    <%= render partial: 'form_fields', locals: {f: f } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
  <% end %>
</div>