<%= main_title(t("action.show", model: Rt::ActivityPublishing.model_name.human)) %>

<div class="row">
  <div class="col-md-6">
    <h3><%= t('common_fields') %></h3>
    <%= render partial: 'request_forms/show', locals: { request_form: @rt_activity_publishing.request_form } %>
  </div>
  <div class="col-md-6">
    <h3><%= t('specific_fields') %></h3>
    <%= render partial: 'show', locals: { rt_activity_publishing: @rt_activity_publishing } %>
  </div>
</div>

<% unless request.xhr? %>
<%= button_to_back %>
<%= button_to_edit(@rt_activity_publishing) %>
<% end %>