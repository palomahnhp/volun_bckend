<tr id="<%= dom_id(rt_volunteer_unsubscribe) %>" >

  <% rt_volunteer_unsubscribe.main_attributes.each do |attr_name| %>
  <td><%= show_attr(rt_volunteer_unsubscribe.public_send attr_name) %></td>
  <% end %>

  <td class="index_table-actions">
    <%= link_to_show(rt_volunteer_unsubscribe) %>
    <%= link_to_edit(rt_volunteer_unsubscribe) %>
<% if rt_volunteer_unsubscribe.try(:archived?) %>
    <%= link_to_recover(rt_volunteer_unsubscribe) %>
<% else %>
    <%= link_to_destroy(rt_volunteer_unsubscribe) %>
<% end %>
  </td>
</tr>
