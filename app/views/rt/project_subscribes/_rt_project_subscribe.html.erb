<tr id="<%= dom_id(rt_project_subscribe) %>" >

  <% rt_project_subscribe.main_attributes.each do |attr_name| %>
  <td><%= show_attr(rt_project_subscribe, attr_name) %></td>
  <% end %>

  <td class="index_table-actions">
    <%= link_to_show(rt_project_subscribe) %>
    <%= link_to_edit(rt_project_subscribe) %>
<% if rt_project_subscribe.try(:archived?) %>
    <%= link_to_recover(rt_project_subscribe) %>
<% else %>
    <%= link_to_destroy(rt_project_subscribe) %>
<% end %>
  </td>
</tr>
