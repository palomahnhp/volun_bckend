<%= main_title(t("action.show", model: Rt::ProjectAction.model_name.human)) %>

<div class="row">
  <div class="col-md-6">
    <h3><%= t('common_fields') %></h3>
    <%= render partial: 'request_forms/show', locals: { request_form: @rt_project_action.request_form } %>
  </div>
  <div class="col-md-6">
    <h3><%= t('specific_fields') %></h3>
    <%= render partial: 'show', locals: { rt_project_action: @rt_project_action } %>
  </div>
</div>

<% unless request.xhr? %>
<%= button_to_back %>
<%= button_to_edit(@rt_project_action) %>
<% end %>