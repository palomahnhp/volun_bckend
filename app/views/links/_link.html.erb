<tr id="<%= dom_id(link) %>" >

  <% link.main_attributes.each do |attr_name| %>
  <td><%= show_attr(link, attr_name) %></td>
  <% end %>

  <td class="index_table-actions">
    <%= link_to_show(link) %>
    <%= link_to_edit(link) %>
<% if link.try(:archived?) %>
    <%= link_to_recover(link) %>
<% else %>
    <%= link_to_destroy(link) %>
<% end %>
  </td>
</tr>
