<%= main_title(t("action.show", model: PtEntity.model_name.human)) %>

<%= render partial: 'show', locals: { pt_entity: @pt_entity } %>

<% unless request.xhr? %>
    <%= button_to_back %>
    <%= button_to_edit(@pt_subvention) %>
<% end %>