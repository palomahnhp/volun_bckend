<div class="well">
  <%= simple_form_for(@user, default_form_options) do |f| %>

  <div class="form-inputs">
          <%= f.input :locale %>
          <%= f.association :profileable %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
  <% end %>
</div>