<tr id="<%= dom_id(project_type_punctual) %>" >

<% project_type_punctual.main_attributes.each do |attr_name| %>
    <% attr = project_type_punctual.public_send(attr_name) %>
    <% if attr.is_a?(TrueClass) || attr.is_a?(FalseClass) %>
        <td><%= t("humanize.#{attr}") %></td>
    <% elsif attr.is_a?(Date) %>
        <td><%= show_simple_date(attr) %></td>
    <% else %>
        <td><%= attr %></td>
    <% end %>
<% end %>

  <td class="index_table-actions">
    <%= link_to_show(project_type_punctual) %>
    <%= link_to_edit(project_type_punctual) %>
<% if project_type_punctual.try(:archived?) %>
    <%= link_to_recover(project_type_punctual) %>
<% else %>
    <%= link_to_destroy(project_type_punctual) %>
<% end %>
  </td>
</tr>
