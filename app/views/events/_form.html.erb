<div class="well">
  <%= simple_form_for(@event, default_form_options) do |f| %>

  <div class="form-inputs">
          <%= f.association :eventable %>
          <%= f.input :address_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
  <% end %>
</div>